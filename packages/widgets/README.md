- DOM-only, zero-dependency widgets for the embed runtime.
- Expose a stable API: `render<Widget>(host, ctx) -> handle.update/destroy`.
- No network code here; the host (Venice) owns submissions/telemetry.


